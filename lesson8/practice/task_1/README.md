# Задача 1: Первая миграция с Goose

## Цель

Научиться работать с миграциями базы данных с помощью библиотеки **goose**:

- создавать каталог для миграций;
- писать SQL-миграции с секциями `Up` и `Down`;
- применять и откатывать миграции.

После выполнения задачи ты:

- создашь структуру проекта с каталогом `migrations/`;
- напишешь первую миграцию для создания таблицы `products`;
- научишься применять миграции к локальной базе данных;
- поймёшь, как работает откат миграций.

---

## Общая идея

Вместо того чтобы вручную выполнять SQL-скрипты для создания таблиц, мы будем использовать **миграции**. Это позволяет:

- версионировать изменения схемы БД;
- легко применять изменения на разных окружениях (dev, staging, prod);
- откатывать изменения при необходимости;
- работать в команде без конфликтов в схеме БД.

---

## Требования

- Go 1.21+
- PostgreSQL 14+
- Docker (опционально)

---

## Часть 1. Настройка базы данных

### Вариант 1: Docker

```bash
docker run --name migrations-practice-postgres \
  -e POSTGRES_USER=user \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=migrations_practice \
  -p 5432:5432 \
  -d postgres:16
```

### Вариант 2: Локальный PostgreSQL

```bash
psql -U postgres -c "CREATE USER \"user\" WITH PASSWORD 'password';"
psql -U postgres -c "CREATE DATABASE migrations_practice OWNER \"user\";"
```

---

## Часть 2. Структура проекта

Создай следующую структуру:

```text
lesson8/
  practice/
    task_1/
      go.mod
      cmd/
        migrate/
          main.go           # Точка входа для запуска миграций
      migrations/
        001_create_products_table.sql   # Первая миграция
```

### 2.1. Инициализация модуля

```bash
cd lesson8/practice/task_1
go mod init migrations_practice
```

### 2.2. Установка зависимостей

```bash
go get github.com/pressly/goose/v3
go get github.com/jackc/pgx/v5/stdlib
```

---

## Часть 3. Создание первой миграции

**Файл:** `migrations/001_create_products_table.sql`

Создай файл миграции со следующей структурой:

```sql
-- +goose Up
-- Здесь пишем SQL для применения миграции (создание таблицы)

-- +goose Down
-- Здесь пишем SQL для отката миграции (удаление таблицы)
```

### Твоя задача

Заполни миграцию так, чтобы:

**В секции `-- +goose Up`:**
- Создавалась таблица `products` с полями:
  - `id` — первичный ключ (SERIAL)
  - `name` — название товара (VARCHAR(255), NOT NULL)
  - `price` — цена (NUMERIC(10, 2), NOT NULL)
  - `quantity` — количество на складе (INT, NOT NULL, по умолчанию 0)
  - `created_at` — дата создания (TIMESTAMP, NOT NULL, по умолчанию NOW())

**В секции `-- +goose Down`:**
- Удалялась таблица `products`

> 💡 Подсказка: используй `CREATE TABLE` и `DROP TABLE IF EXISTS`.

---

## Часть 4. Программа для запуска миграций

**Файл:** `cmd/migrate/main.go`

Напиши программу, которая:

1. Читает строку подключения из переменной окружения `DATABASE_URL` (или использует значение по умолчанию).
2. Подключается к базе данных.
3. Принимает команду через флаг `-command` (up, down, status, version).
4. Выполняет соответствующую команду goose.

### Заготовка

```go
package main

import (
    "database/sql"
    "flag"
    "fmt"
    "log"
    "os"

    _ "github.com/jackc/pgx/v5/stdlib"
    "github.com/pressly/goose/v3"
)

const migrationsDir = "migrations"

func main() {
    // TODO: определить флаг -command со значением по умолчанию "up"

    // TODO: получить DSN из переменной окружения DATABASE_URL
    // Если переменная пустая, использовать значение по умолчанию:
    // "postgres://user:password@localhost:5432/migrations_practice?sslmode=disable"

    // TODO: открыть соединение с БД через sql.Open("pgx", dsn)

    // TODO: проверить соединение через db.Ping()

    // TODO: выполнить команду goose в зависимости от значения флага -command
}

func runGooseCommand(db *sql.DB, command string) error {
    switch command {
    case "up":
        return goose.Up(db, migrationsDir)
    case "down":
        return goose.Down(db, migrationsDir)
    case "status":
        return goose.Status(db, migrationsDir)
    case "version":
        version, err := goose.GetDBVersion(db)
        if err != nil {
            return err
        }
        fmt.Printf("Current DB version: %d\n", version)
        return nil
    default:
        return fmt.Errorf("unknown command: %s", command)
    }
}
```

---

## Часть 5. Проверка решения

### 5.1. Применение миграции

```bash
go run ./cmd/migrate -command=up
```

Ожидаемый вывод:
```
OK   001_create_products_table.sql
goose: successfully migrated database to version: 1
```

### 5.2. Проверка статуса

```bash
go run ./cmd/migrate -command=status
```

Ожидаемый вывод:
```
    Applied At                  Migration
    =======================================
    Sat Jan 10 12:00:00 2024 -- 001_create_products_table.sql
```

### 5.3. Проверка в базе данных

Подключись к БД и проверь, что таблица создана:

```bash
psql -U user -d migrations_practice -c "\dt"
```

Ожидаемый вывод:
```
              List of relations
 Schema |       Name        | Type  | Owner
--------+-------------------+-------+-------
 public | goose_db_version  | table | user
 public | products          | table | user
```

Проверь структуру таблицы:

```bash
psql -U user -d migrations_practice -c "\d products"
```

### 5.4. Откат миграции

```bash
go run ./cmd/migrate -command=down
```

Ожидаемый вывод:
```
OK   001_create_products_table.sql
```

Проверь, что таблица `products` удалена:

```bash
psql -U user -d migrations_practice -c "\dt"
```

Должна остаться только таблица `goose_db_version`.

### 5.5. Повторное применение

```bash
go run ./cmd/migrate -command=up
```

Миграция должна снова успешно примениться.

---

## Дополнительные задания

1. ⭐ Добавь вторую миграцию `002_add_description_to_products.sql`, которая добавляет колонку `description TEXT` в таблицу `products`.

2. ⭐⭐ Добавь команду `up-one` для применения только одной следующей миграции (используй `goose.UpByOne`).

3. ⭐⭐⭐ Добавь команду `reset` для полного отката всех миграций (используй `goose.Reset`).

---

## Частые ошибки

### Ошибка: "no migrations found"

Убедись, что:
- Каталог `migrations/` существует
- Файлы миграций имеют расширение `.sql`
- Ты запускаешь программу из корня проекта (где находится каталог `migrations/`)

### Ошибка: "connection refused"

Убедись, что:
- PostgreSQL запущен
- Порт 5432 доступен
- Данные для подключения (user, password, database) корректны

### Ошибка: "syntax error in migration"

Проверь:
- Наличие комментариев `-- +goose Up` и `-- +goose Down`
- Корректность SQL-синтаксиса

---

Если миграция успешно применяется и откатывается, а таблица `products` создаётся с правильной структурой — задание выполнено! 🎉
