

# Задача 2: Рефакторинг монолита в слоистое приложение

## Цель

Закрепить на практике:

- понимание, **почему плохо**, когда вся логика свалена в один файл и один handler;
- умение **разносить код по слоям**: `handlers → service → repository → entities`;
- роль `main.go` как **точки входа и сборщика** приложения, а не места для всей логики;
- сохранение поведения приложения при изменении архитектуры (рефакторинг без изменения внешнего API).

## Что уже есть

В этой задаче тебе уже дан готовый файл `main.go` (в этой же папке `task_2`), в котором:

- объявлена сущность `Note`;
- глобальные переменные `notes` и `nextID` используются как "псевдо-БД";
- вся логика работы с заметками (`GET`, `POST`, `DELETE`) реализована **в одном хендлере** `notesHandler`;
- в `main()` создаётся роутер и регистрируется только один путь `/notes`.

Это пример **"анти‑архитектуры"** — всё в одном месте, нарушено разделение ответственности.

Твоя задача — **переписать это приложение в слоистую архитектуру**, не меняя внешнее поведение.

## Условие

Текущий сервер умеет обрабатывать три варианта запросов на `/notes`:

1. `GET /notes`  
   - Возвращает список заметок в виде JSON‑массива.  
   - Статус: `200 OK`.

2. `POST /notes`  
   - Принимает JSON вида:
     ```json
     {
       "text": "Buy milk"
     }
     ```
   - Создаёт новую заметку с уникальным `id` на основе `nextID`.  
   - Добавляет её в слайс `notes`.  
   - Возвращает созданную заметку в JSON.  
   - Статус: `201 Created`.  
   - Если `text` пустой или JSON невалидный — возвращает `400 Bad Request`.

3. `DELETE /notes?id=1`  
   - Удаляет заметку с указанным `id` из слайса `notes`.  
   - Если заметка найдена и удалена — возвращает пустой ответ со статусом `204 No Content`.  
   - Если заметка не найдена — возвращает `404 Not Found` с сообщением об ошибке.  
   - Если `id` не передан или не является числом — `400 Bad Request`.

**Ваша задача** — переписать этот код в **слоистую архитектуру**, сохранив:

- все три метода (`GET`, `POST`, `DELETE`);
- тот же URL (`/notes`);
- те же статус‑коды и формат ответов (по смыслу).

## Требования к структуре проекта

Перенеси логику из существующего `main.go` в слои и папки:

```text
lesson3/
  practice/
    task_2/
      cmd/
        api/
          main.go           // точка входа
      internal/
        entities/
          note.go           // сущность Note
        repository/
          note_repo.go      // хранилище заметок (in-memory)
        service/
          note_service.go   // бизнес-логика для заметок
        handlers/
          note_handler.go   // HTTP-хендлеры для /notes
```

Рекомендации:

- текущий `main.go` из корня `task_2` можно:
  - либо перенести и переписать в `cmd/api/main.go`,
  - либо оставить как «до рефакторинга», а новый `main.go` собрать отдельно в `cmd/api`;
- важно, чтобы **после рефакторинга** приложение запускалось через `go run ./cmd/api` (или аналогично) и вело себя так же, как исходная версия.

## Требования к слоям

### 1. `entities` — сущность Note

Создай структуру `Note` в `internal/entities/note.go`:

- поля: `ID int`, `Text string`;
- теги JSON должны соответствовать текущему API (`id`, `text`).

### 2. `repository` — работа с данными

Создай репозиторий заметок в `internal/repository/note_repo.go`.

Он должен:

- хранить заметки в памяти (in‑memory);
- генерировать новый `ID` для каждой созданной заметки;
- уметь:
  - добавить заметку;
  - вернуть все заметки;
  - удалить заметку по `id` с информацией, была ли она найдена.

Ты можешь повторно использовать ту же логику, которая сейчас написана внутри `notesHandler`, но перенести её в методы репозитория.

### 3. `service` — бизнес‑логика

Создай сервис заметок в `internal/service/note_service.go`.

Он должен:

- использовать репозиторий через интерфейс (интерфейс можно объявить рядом);
- предоставлять методы:
  - `CreateNote(text string)` — с проверкой текста (не пустой);
  - `ListNotes()` — получить список заметок;
  - `DeleteNote(id int)` — удалить заметку по id.

Часть проверок, которые сейчас живут в handler’е, можно перенести в сервис (например, «пустой текст»).

### 4. `handlers` — HTTP‑слой

Создай хендлер для заметок в `internal/handlers/note_handler.go`.

Он должен:

- иметь метод `NotesHandler(w http.ResponseWriter, r *http.Request)`;
- внутри по `r.Method` вызывать разные ветки:
  - `GET` → вызвать метод сервиса для списка и вернуть JSON + `200 OK`;
  - `POST` → прочитать JSON, вызвать сервис для создания, вернуть JSON + `201 Created`;
  - `DELETE` → прочитать `id` из query, вызвать сервис для удаления, вернуть `204` или `404`;
- содержать только логику HTTP:
  - чтение `r.Body` / параметров запроса;
  - выбор статус‑кодов;
  - сериализацию в JSON.

Хендлер **не должен** сам управлять глобальными переменными и слайсами — только через сервис.

### 5. `cmd/api/main.go` — точка входа

В `cmd/api/main.go`:

- создай репозиторий;
- создай сервис поверх репозитория;
- создай хендлер поверх сервиса;
- настрой роутер и зарегистрируй `/notes`:
  ```go
  mux := http.NewServeMux()
  mux.HandleFunc("/notes", noteHandler.NotesHandler)
  ```
- запусти сервер на `:8080`.

В `main.go` **не должно быть бизнес‑логики** задач, только «склейка» слоёв и запуск сервера.

## Подсказки

- Начни с того, чтобы аккуратно посмотреть на текущий `notesHandler` и понять, **какую ответственность** можно вынести:
  - работа с памятью (`notes`, `nextID`) → в репозиторий;
  - проверка данных и сценарий «создать/удалить/получить» → в сервис;
  - HTTP‑детали (чтение JSON, статус‑коды, заголовки) → оставить в handler.
- Сначала перенеси сущность `Note` в `internal/entities` и убедись, что всё компилируется.
- Потом вытащи in‑memory‑хранилище в `repository`, затем сервис, а в конце — подчисти handler.

## Как проверить решение

1. Перейди в папку задачи:

   ```bash
   cd bitlab_golang_module_3/lesson3/practice/task_2
   ```

2. Запусти сервер через новый `main` (например, из `cmd/api`):

   ```bash
   go run ./cmd/api
   ```

3. Проверь, что API ведёт себя так же, как исходный вариант до рефакторинга:

   - `GET /notes` — список заметок;
   - `POST /notes` с телом `{"text":"Buy milk"}` — создаёт заметку и возвращает её с `id` и статусом `201`;
   - `DELETE /notes?id=1` — удаляет заметку и возвращает `204 No Content`;
   - ошибки (`400`, `404`, `405`) по тем же правилам, что и раньше.

4. Убедись, что:

   - в `main.go` нет бизнес‑логики;
   - handler не содержит прямой работы с глобальными переменными;
   - вся логика работы с данными находится в `repository`/`service`.

Если поведение сервера осталось прежним, а код стал разделённым по слоям — задача выполнена 👍