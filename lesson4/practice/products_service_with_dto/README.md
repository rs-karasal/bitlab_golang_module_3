# Продукты: сущности, DTO и JSON

## Цель

Закрепить на практике тему лекции **«Сущности, DTO и JSON»** на примере простой сервис‑ручки для управления товарами.

После выполнения задачи ты:

- опишешь доменную сущность `Product` и её простую валидацию;
- создашь DTO‑структуры для запроса и ответа;
- реализуешь ручку `POST /products`, которая работает с JSON и слоистой архитектурой (handler → service → entities).

Проектный шаблон уже подготовлен, тебе нужно **дописать детали по TODO**.

---

## Общая идея задачи

У нас есть сервис товаров (products service).  
Каждый товар описывается сущностью `Product`:

- `ID` — идентификатор товара;
- `Name` — название;
- `Price` — цена;
- `Quantity` — количество на складе.

Мы хотим:

1. На уровне **entities** описать сущность `Product` и её метод `Validate()`.
2. На уровне **DTO** описать структуры:
   - `CreateProductRequest` — тело запроса `POST /products`;
   - `ProductResponse` — ответ сервиса.
3. На уровне **handlers** реализовать хендлер `POST /products`, который:
   - читает JSON из тела запроса в `CreateProductRequest`;
   - превращает DTO в сущность `Product`;
   - передаёт `Product` в сервис;
   - получает от сервиса созданный `Product`;
   - превращает его в `ProductResponse`;
   - отправляет JSON‑ответ со статусом `201 Created`.

---

## Структура проекта

Примерная структура (может слегка отличаться от твоего шаблона, но идея та же):

```text
lesson4/
  practice/
    products_service_with_dto/
      go.mod
      cmd/
        api/
          main.go
      internal/
        entities/
          product.go            // доменная сущность Product
        service/
          product_service.go    // бизнес-логика для продуктов
        handlers/
          product_handler.go    // HTTP-хендлеры для /products
          dto/
            product_dto.go      // DTO для запросов/ответов
```

В шаблоне проекта уже должны быть заготовки файлов и TODO‑комментарии.  
Твоя задача — **дописать реализацию в нужных местах**.

---

## Часть 1. Сущность и метод Validate

**Файл:** `internal/entities/product.go`

### Задача

Описать доменную сущность `Product` и метод её валидации.

### Требования

1. Создать структуру `Product` со следующими полями:

   ```go
   type Product struct {
       ID       int
       Name     string
       Price    float64
       Quantity int
   }
   ```

2. Реализовать метод `Validate()` у сущности `Product`, который проверяет:

   - `Name` не пустой;
   - `Price` >= 0;
   - `Quantity` >= 0.

3. При нарушении условий метод должен возвращать ошибку, при корректных данных — `nil`.

### Подсказка

В шаблоне может быть заготовка вида:

```go
package entities

import "errors"

var (
    ErrEmptyName    = errors.New("name is empty")
    ErrNegativePrice = errors.New("price cannot be negative")
    ErrNegativeQuantity = errors.New("quantity cannot be negative")
)

type Product struct {
    // TODO: добавить поля ID, Name, Price, Quantity
}

func (p Product) Validate() error {
    // TODO: реализовать проверки и вернуть соответствующие ошибки
    return nil
}
```

Тебе нужно **заменить TODO на рабочий код**.

---

## Часть 2. DTO для создания товара

**Файл:** `internal/handlers/dto/product_dto.go` (или аналогичный в папке `dto`).

### Задача

Создать структуры для работы с JSON‑запросами/ответами и функции маппинга между DTO и Entity.

### Требования

1. Описать DTO для запроса `CreateProductRequest`:

   ```go
   type CreateProductRequest struct {
       Name     string  `json:"name"`
       Price    float64 `json:"price"`
       Quantity int     `json:"quantity"`
   }
   ```

2. Описать DTO для ответа `ProductResponse`:

   ```go
   type ProductResponse struct {
       ID       int     `json:"id"`
       Name     string  `json:"name"`
       Price    float64 `json:"price"`
       Quantity int     `json:"quantity"`
   }
   ```

3. Реализовать функции:

   ```go
   func NewProductFromCreateRequest(req CreateProductRequest) entities.Product {
       // TODO: создать Product из DTO-запроса (без ID)
   }

   func NewProductResponse(p entities.Product) ProductResponse {
       // TODO: создать DTO-ответ из сущности Product
   }
   ```

   Обрати внимание:

   - `CreateProductRequest` не содержит `ID` — его может присваивать сервис/репозиторий;
   - в `ProductResponse` ID уже должен быть.

### Подсказка

В шаблоне могут быть уже описаны структуры и функция с TODO — тебе нужно дописать логику маппинга.

---

## Часть 3. Хендлер с JSON: POST /products

**Файл:** `internal/handlers/product_handler.go`

### Задача

Реализовать HTTP‑хендлер, который принимает JSON‑запрос, вызывает сервис и возвращает JSON‑ответ.

### Что должен делать хендлер

1. Принимать запрос `POST /products`.

2. Читать тело запроса и декодировать JSON в `CreateProductRequest`:

   - при невалидном JSON вернуть `400 Bad Request` и сообщение об ошибке.

3. Преобразовать `CreateProductRequest` в `entities.Product` с помощью `NewProductFromCreateRequest`.

4. Передать `Product` в сервис (например, метод `CreateProduct` или `AddProduct`).

5. Проверить ошибку валидации:

   - если `Validate()` вернул ошибку — вернуть `400 Bad Request` и сообщение об ошибке;
   - если произошла внутренняя ошибка сервиса/хранилища — вернуть `500 Internal Server Error`.

6. Если всё успешно:

   - получить из сервиса созданный `Product` (с присвоенным `ID`);
   - преобразовать его в `ProductResponse` с помощью `NewProductResponse`;
   - вернуть JSON и статус `201 Created`.

### Подсказка по структуре кода

В хендлере тебе пригодятся:

- `json.NewDecoder(r.Body).Decode(&req)` — чтение JSON из тела запроса;
- `w.Header().Set("Content-Type", "application/json")` — указать тип ответа;
- `w.WriteHeader(http.StatusCreated)` — установить статус;
- `json.NewEncoder(w).Encode(resp)` — отправить JSON‑ответ.

Важно: в хендлере **не должно быть бизнес-логики** (валидации полей, присвоения ID и т.п.) — только работа с HTTP и вызов сервиса + маппинг DTO ↔ Entity.

---

## Как проверить решение

1. Перейди в папку проекта:

   ```bash
   cd bitlab_golang_module_3/lesson4/practice/products_service_with_dto
   ```

2. Запусти сервер (обычно через `cmd/api`):

   ```bash
   go run ./cmd/api
   ```

3. Отправь запрос на создание товара:

   ```bash
   curl -X POST "http://localhost:8080/products" \
        -H "Content-Type: application/json" \
        -d '{"name":"Laptop","price":1200.5,"quantity":10}'
   ```

   Ожидаемый результат (пример):

   ```json
   {
     "id": 1,
     "name": "Laptop",
     "price": 1200.5,
     "quantity": 10
   }
   ```

   Статус должен быть `201 Created`.

4. Попробуй отправить некорректные данные:

   - пустое имя;
   - отрицательную цену;
   - отрицательное количество;
   - сломанный JSON.

   Убедись, что хендлер возвращает **корректные статус‑коды** (`400`, `500`) и понятные сообщения об ошибке.

5. ⭐ По желанию: добавь `GET /products` и `GET /products/{id}`, используя те же сущности и DTO.

---

Если ты всё сделал, и запрос `POST /products` работает как описано, — значит, ты отлично разобрался с сущностями, DTO и JSON в контексте слоистой архитектуры 💪
